FROM oven/bun:1-alpine

WORKDIR /app

# Install deps first for better layer caching
COPY package.json bun.lock ./
RUN bun install --frozen-lockfile

# Copy source (will be overlaid by bind mount in dev)
COPY . .

ENV HOST=0.0.0.0
ENV PORT=5173

EXPOSE 5173

CMD ["bun", "run", "dev"]
